// import api functions that retrieve db information, like the user's recipes and their family's recipes

export default async function Page(){
    // retrieve profile's information
    // And their recipes. And family recipes (these ones are not editable)
    return(
        <p>Hello world</p>
    );
}